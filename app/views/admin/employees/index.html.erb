<% content_for :left do %>
	<%= render :partial => 'admin/companies/menu', :locals => {:active => :employees} %>
<% end %>

<%= simple_list(:employee, @employees, {:column => "firstname, lastname"}) %>
